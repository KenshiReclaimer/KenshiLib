#include "kenshi/inventory.h"

/*
const std::string InventorySection::Hip = "hip";
const std::string InventorySection::Back = "back";
const std::string InventorySection::Boots = "boots";
const std::string InventorySection::Head = "head";
const std::string InventorySection::Backpack_Attach = "backpack_attach";
const std::string InventorySection::Shirt = "shirt";
const std::string InventorySection::Armour = "armour";
const std::string InventorySection::Legs = "legs";
const std::string InventorySection::Belt = "belt";
const std::string InventorySection::Main = "main";

InventorySection* Inventory::GetInventorySection(std::string sectionName)
{
    for (int i = 0; i < inventorySections.size(); ++i)
    {
        if (inventorySections[i]->sectionName == sectionName)
            return inventorySections[i];
    }
    return nullptr;
}

*/